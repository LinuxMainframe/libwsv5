\hypertarget{Using-example}{}\doxysection{Using}
Free a scene list array returned by obsws\+\_\+get\+\_\+scene\+\_\+list().

Free a scene list array returned by obsws\+\_\+get\+\_\+scene\+\_\+list().When you call obsws\+\_\+get\+\_\+scene\+\_\+list(), it allocates memory for the scene names and the array itself. You must free all of this using this function when done. Don\textquotesingle{}t try to free the individual strings manually or use plain free() -\/ that won\textquotesingle{}t work correctly.

Why have a special function for this instead of just free()? Because the memory layout needs special handling -\/ there\textquotesingle{}s an array of pointers, each pointing to separately allocated strings.


\begin{DoxyParams}{Parameters}
{\em scenes} & Array of scene name strings (from obsws\+\_\+get\+\_\+scene\+\_\+list) \\
\hline
{\em count} & Number of scenes in the array\\
\hline
\end{DoxyParams}
\begin{DoxyNote}{Note}
Safe to call with NULL scenes pointer (does nothing). 

After calling this, the scenes pointer is invalid -\/ don\textquotesingle{}t use it anymore. 

The count parameter must match what obsws\+\_\+get\+\_\+scene\+\_\+list() returned.
\end{DoxyNote}
and freeing scene list\+: char $\ast$$\ast$scenes = NULL; size\+\_\+t count = 0; if (obsws\+\_\+get\+\_\+scene\+\_\+list(conn, \&scenes, \&count) == OBSWS\+\_\+\+OK) \{ for (size\+\_\+t i = 0; i \texorpdfstring{$<$}{<} count; i++) \{ printf(\char`\"{}\+Scene\+: \%s\textbackslash{}n\char`\"{}, scenes\mbox{[}i\mbox{]}); \} obsws\+\_\+free\+\_\+scene\+\_\+list(scenes, count); // Must do this! \}


\begin{DoxyCodeInclude}{0}

\end{DoxyCodeInclude}
 